export enum AuthenticationMethodType {
    CREDENTIALS = 'credentials',
    SSO_CLEVER = 'sso-clever',
    CLASS_CODE = 'class-code',
    CUSTOM_REQUEST = 'custom-request',
    REFRESH = 'token-refresh',
}